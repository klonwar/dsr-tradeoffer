import React, { FC } from 'react';

export const ShowCatalogueItemPage: FC = () => {
  return (
    <h4 className={`uk-position-center uk-margin-remove uk-text-muted`}>
      Пока не реализовано
    </h4>
  );
};